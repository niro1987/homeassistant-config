---
#
#
#
path: f1
title: First Floor
theme: Google Light Theme
icon: mdi:bed-empty
panel: false
cards:
  - type: vertical-stack
    cards:
      # Message
      # - type: markdown
      #   content: |
      #     {% if now().hour < 5 %}
      #       # Goede nacht, {{user}}!
      #     {% elif now().hour < 12 %}
      #       # Goede morgen, {{user}}!
      #     {% elif now().hour < 18 %}
      #       # Goede middag, {{user}}!
      #     {% else %}
      #       # Fijne avond, {{user}}!
      #     {% endif %}
      #   style:
      #     .: |
      #       ha-card {
      #         margin: 0px 14px 0px 10px;
      #         box-shadow: none;
      #         --ha-card-background: rgba(0, 0, 0, 0.0)
      #       }
      #     ha-markdown:
      #       $: |
      #         h1 {
      #           font-size: 20px;
      #           font-weight: 500;
      #           color: var(--app-header-text-color);
      #         }
      # Date
      - type: markdown
        content: |
          {% set ramen = expand(states.group.ramen) | selectattr('state', 'eq', 'on') | map(attribute='name') | list %}
          {% if ramen | count == 1 %}
            # Het {{ramen[0] | lower}} staat open
          {% elif ramen | count >= 1 %}
            # Er staan {{ramen | count}} ramen open
          {% else %}
            # Alle ramen zijn gesloten
          {% endif %}
        style:
          .: |
            ha-card {
              margin: 12px 14px -5px 14px;
              box-shadow: none;
              --ha-card-background: rgba(0, 0, 0, 0.0)
            }
          ha-markdown:
            $: |
              h1 { 
                font-size: 18px !important;
                font-weight: 400 !important;
                padding-left: 10px !important;
                border-left: 3px solid rgba(81, 134, 236);
              }
      # Trash
      # - type: custom:state-switch
      #   entity: fan.afzuiging
      #   states:
      #     'on':
      #       type: entities
      #       style: |
      #         ha-card {
      #           margin: 0px 14px 0px 14px;
      #           background: rgba(0, 39, 168, 0.1);
      #         }
      #       entities:
      #         - entity: fan.afzuiging
      # Lights
      - type: entities
        show_header_toggle: false
        style: |
          ha-card {
            margin: 0px 14px 0px 14px;
          }
        entities:
          - type: custom:fold-entity-row
            head: group.lampen_slaapkamer
            entities:
              - entity: light.slaapkamer_danielle
                icon: mdi:wall-sconce-round
              - entity: light.slaapkamer_niels
                icon: mdi:wall-sconce-round
              - entity: light.slaapkamer_plafond
                icon: mdi:ceiling-light
          - type: custom:fold-entity-row
            head: group.lampen_slaapkamer_kids
            open: true
            entities:
              - entity: light.lily_plafond
                icon: mdi:flower
              - entity: light.olivia_plafond
                icon: mdi:sprout
